import { createTheme } from '@medyanes360/tasarim-sistemi';

/**
 * UYGULAMA TEMASI.
 * Yeni uygulama açarken görsel kimlik BURADAN değiştirilir; tasarım sistemi
 * bileşenlerine dokunulmaz (TASARIM.md §1 — tema kıyafet, bileşenler vücut).
 * Şablon, fabrika varsayılan temasını olduğu gibi kullanır.
 */
export const uygulamaTemasi = createTheme({
  // Örnek: marka rengi değiştirmek için yorum satırını açıp düzenleyin.
  // renkler: { light: { birincil: '#7C3AED' }, dark: { birincil: '#8B5CF6' } },
});
